name: Tests

on: 
  pull_request:
  push:
    branches-ignore:
      - 'releases/**'
  workflow_call:

jobs:
  Run-unit-tests:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install modules
      run: npm install
    - name: Run tests
      run: echo 'npm run test'